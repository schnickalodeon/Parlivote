<div class="d-flex flex-row my-3">
    <h3 class="me-4">Sitzungen </h3>
    <ChairAuthorization>
        <ButtonBase Text="Sitzung hinzufügen" OnClick="@(() => addMeetingComponent.Show())" />
    </ChairAuthorization>
</div>

<StateEvaluation State="@this.state">
    <Loading>
        <p>Loading....</p>
    </Loading>

    <Content>
        <Accordion>
            <Iterations Items="meetings">
                <MeetingBox Meeting="@context" OnMeetingChanged="LoadMeetings" />
            </Iterations>
        </Accordion>
    </Content>
    
    <Error>
        <div class="alert alert-danger">
            @error
        </div>
    </Error>
</StateEvaluation>

<AddMeetingComponent @ref="addMeetingComponent" OnMeetingAdded="LoadMeetings" />